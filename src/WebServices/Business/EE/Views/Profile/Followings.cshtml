@model Aiursoft.EE.Models.ProfileViewModels.FollowingsViewModel
@{
    ViewData["Title"] = "Followings";
}
<div class="container">
    <div class="row">
        @(await Html.PartialAsync("_Left", Model.LeftContent))
        <div class="col-md-9 mt-3">
            @(await Html.PartialAsync("_Up", Model.UpContent))
            <div class="row">
                @foreach (var fol in Model.Followings)
                {
                    <div class="col-sm-8">
                        <img class="aiuruser-img mt-3" src="@await ProbeLocator.GetProbeOpenAddressAsync(fol.Receiver.IconFilePath)" style="width: 20px; height:20px;"/>
                        <a asp-controller="Profile" asp-action="Overview" asp-route-id="@fol.Receiver.UserName">
                            <h3 class="mt-3">@fol.Receiver.NickName</h3>
                        </a>
                        <p>Lorem ipsum dolor sit amet.</p>
                    </div>
                    <div class="col-sm-2">
                        @if (Model.IsMe)
                        {
                            <a class="btn btn-danger mt-3" id="follow" href="javascript:unFollow('@fol.ReceiverId')">
                                UnFollow
                            </a>
                        }
                    </div>
                    <hr/>
                }
            </div>
        </div>
    </div>
</div>