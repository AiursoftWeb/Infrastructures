@model Aiursoft.Wrap.Models.DashboardViewModels.DeleteViewModel
@{
    ViewData["Title"] = "Delete record - " + Model.RecordName;
}

<div class="container-fluid">
    <ol class="breadcrumb">
        <li class="breadcrumb-item">
            <a asp-controller="Dashboard" asp-action="Index">@Localizer["Dashboard"]</a>
        </li>
        <li class="breadcrumb-item">
            <a asp-controller="Dashboard" asp-action="Records">@Localizer["My records"]</a>
        </li>
        <li class="breadcrumb-item active">@ViewData["Title"]</li>
    </ol>

    <div class="alert alert-danger alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">x</span></button>
        <strong>@Localizer["Caution!"]</strong> Are you about to delete record "@Model.RecordName"?
    </div>

    <div class="card mb-3">
        <div class="card-header">
            <i class="fas fa-fw fa-trash-alt ico"></i>
            @Localizer["Delete record"]
        </div>
        <div class="card-body">
            <div class="col-12">
                <form asp-controller="Dashboard" asp-action="Delete" asp-route-recordName="@Model.RecordName" method="post">
                    <div class="row">
                        <div class="col-md-4">
                            <div class="form-group">
                                <label asp-for="RecordName"></label>
                                <input asp-for="RecordName" type="text" class="form-control" readonly="readonly">
                            </div>
                        </div>

                        <div class="col-md-12 margin-top-30">
                            <a class="btn btn-success" asp-controller="Dashboard" asp-action="Records">@Localizer["Back to all records"]</a>
                            <button class="btn btn-danger" data-disable-with="Deleting..." type="submit">@Localizer["Confirm to delete"]</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>