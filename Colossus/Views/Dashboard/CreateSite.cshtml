@model Aiursoft.Colossus.Models.DashboardViewModels.CreateSiteViewModel
@{
    ViewData["Title"] = Model.Title;
}
<div class="container-fluid">
    <ol class="breadcrumb">
        <li class="breadcrumb-item">
            <a asp-controller="Dashboard" asp-action="Index">@Localizer["Dashboard"]</a>
        </li>
        <li class="breadcrumb-item active">@ViewData["Title"]</li>
    </ol>
    <div class="card mb-3">
        <div class="card-header">
            <i class="fa fa-inbox"></i>
            @Localizer["Create new site"]
        </div>
        <div class="card-body">
            <div class="col-12">
                <form asp-controller="Dashboard" asp-action="CreateSite" method="post" class="row">

                    <div class="col-md-4">
                        <div class="form-group">
                            <label asp-for="SiteName"></label>
                            <input asp-for="SiteName" type="text" class="form-control" id="TxtSiteName" placeholder="Enter your new site name...">
                            <span asp-validation-for="SiteName" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="col-md-12 margin-top-30">
                        <button type="submit" data-disable-with="Creating..." class="btn btn-primary btn">@Localizer["Create"]</button>
                        <button type="reset" class="btn btn-danger btn" id="BtnClear" onclick="ClkClear()">@Localizer["Clear"]</button>
                    </div>

                </form>
            </div>
        </div>
    </div>
</div>
@section js{
    <script src="@Url.Content("~/Scripts/jquery-3.3.1.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/jquery-ui-1.12.1.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/jquery.unobtrusive-ajax.js")" type="text/javascript"></script>
    <script>
        var ClkClear = function () {
            $('#TxtSiteName').val = "Enter your new site name...";
          //  alert(1221);
        }
    </script>
}